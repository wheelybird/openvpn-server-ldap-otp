#%PAM-1.0

# Standalone LDAP+TOTP authentication module
# Single module performs both password authentication (via LDAP bind)
# and TOTP validation (reading secret from LDAP)
auth required pam_ldap_totp_auth.so

account sufficient pam_permit.so
